# Backend README UTF-8 迁移说明

后端正式说明已统一维护在 [`README.md`](README.md)。

本文件曾用于保留编码修正版 README。为避免两份后端文档内容分叉，当前仅保留迁移提示：

- 后端入口、目录结构、API 前缀、服务层、引擎层、Celery 队列和开发命令请阅读 `backend/README.md`。
- 所有 README 均应使用 UTF-8 编码。
- 修改文档后运行仓库根目录的 `python scripts/check_utf8.py`。
